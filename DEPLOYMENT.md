# 🚀 Инструкция по развертыванию проекта knigavl.ru

## 📍 Текущее расположение проекта
```
/root/snap/knigavl.ru/
```

## ✅ Шаг 1: Проверка Node.js и npm

Убедитесь, что на сервере установлены Node.js (v14+) и npm:
```bash
node --version
npm --version
```

Если не установлены, установите Node.js:
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

## ✅ Шаг 2: Установка PM2 (если еще не установлен)

PM2 нужен для управления процессом Node.js:
```bash
sudo npm install -g pm2
```

## ✅ Шаг 3: Установка зависимостей

Перейдите в директорию проекта и установите все зависимости:
```bash
cd ~/snap/knigavl.ru
npm install
```

⚠️ **Важно**: Установка может занять 5-10 минут из-за больших библиотек (React, Three.js и др.)

## ✅ Шаг 4: Проверка .env файла

Убедитесь, что файл `.env` существует и содержит все необходимые переменные:
```bash
cat .env
```

Должны присутствовать:
- `MYSQL_HOST=185.225.35.229`
- `MYSQL_USER=zxc`
- `MYSQL_PASSWORD=Sereza11`
- `MYSQL_DATABASE=db`
- `MAIN_BOT_TOKEN=...`
- `SUPPORT_BOT_TOKEN=...`
- `PORT=10001`
- `NODE_ENV=production`

## ✅ Шаг 5: Тестирование подключения к MySQL

Проверьте, что MySQL доступна:
```bash
mysql -h 185.225.35.229 -u zxc -pSereza11 db -e "SHOW TABLES;"
```

Если подключение работает, продолжайте.

## ✅ Шаг 6: Сборка React фронтенда

Соберите фронтенд для production:
```bash
npm run build
```

⚠️ **Важно**: Сборка может занять 5-15 минут и использовать много памяти. Убедитесь, что у сервера достаточно RAM (минимум 2GB).

После сборки должна появиться папка `build/` с файлами фронтенда.

## ✅ Шаг 7: Запуск приложения через PM2

Запустите приложение:
```bash
pm2 start ecosystem.config.js
```

Проверьте статус:
```bash
pm2 status
```

Посмотрите логи:
```bash
pm2 logs vl-app
```

## ✅ Шаг 8: Настройка автозапуска PM2

Чтобы приложение автоматически запускалось после перезагрузки сервера:
```bash
pm2 startup
pm2 save
```

## ✅ Шаг 9: Настройка Nginx

### Вариант A: Если Nginx уже установлен на другом сервере

Убедитесь, что Nginx настроен на проксирование запросов на `185.225.35.229:10001` (IP вашего Node.js сервера).

### Вариант B: Если Nginx нужно установить на этом же сервере

1. Установите Nginx:
```bash
sudo apt update
sudo apt install nginx -y
```

2. Создайте конфигурацию:
```bash
sudo nano /etc/nginx/sites-available/knigavl.ru
```

3. Вставьте конфигурацию (упрощенная версия):
```nginx
server {
    listen 80;
    server_name knigavl.ru www.knigavl.ru;
    
    root /root/snap/knigavl.ru/build;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api/ {
        proxy_pass http://127.0.0.1:10001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

4. Активируйте конфигурацию:
```bash
sudo ln -s /etc/nginx/sites-available/knigavl.ru /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

## ✅ Шаг 10: Проверка работоспособности

### Проверка API:
```bash
curl http://localhost:10001/api/health
```

Должен вернуть JSON с статусом `"status": "ok"` и `"mysql": "connected"`.

### Проверка Telegram ботов:

1. Откройте Telegram
2. Найдите основного бота и отправьте `/start`
3. Найдите бота поддержки и отправьте `/start`

Боты должны ответить приветственными сообщениями.

### Проверка веб-приложения:
```bash
curl http://localhost
```

Должна вернуться HTML страница.

## 📊 Полезные команды PM2

```bash
# Посмотреть статус
pm2 status

# Посмотреть логи
pm2 logs vl-app

# Остановить приложение
pm2 stop vl-app

# Перезапустить приложение
pm2 restart vl-app

# Удалить приложение из PM2
pm2 delete vl-app

# Посмотреть использование ресурсов
pm2 monit
```

## 🔍 Диагностика проблем

### Если приложение не запускается:

1. Проверьте логи PM2:
```bash
pm2 logs vl-app --lines 100
```

2. Проверьте, что порт 10001 свободен:
```bash
sudo netstat -tlnp | grep 10001
```

3. Проверьте подключение к MySQL:
```bash
mysql -h 185.225.35.229 -u zxc -pSereza11 db -e "SELECT 1;"
```

### Если Telegram боты не отвечают:

1. Проверьте логи на наличие ошибок:
```bash
pm2 logs vl-app | grep -i "telegram\|bot"
```

2. Убедитесь, что токены ботов корректны в `.env`

3. Проверьте, что боты не запущены в другом месте (конфликт polling)

### Если фронтенд не загружается:

1. Проверьте, что папка `build/` существует и содержит файлы:
```bash
ls -la ~/snap/knigavl.ru/build/
```

2. Проверьте права доступа:
```bash
chmod -R 755 ~/snap/knigavl.ru/build/
```

3. Проверьте конфигурацию Nginx:
```bash
sudo nginx -t
```

## 🔄 Обновление приложения

После изменения кода:

1. Для изменений в бэкенде:
```bash
pm2 restart vl-app
```

2. Для изменений в фронтенде:
```bash
npm run build
# Nginx автоматически отдаст новые файлы
```

3. Для изменений в зависимостях:
```bash
npm install
pm2 restart vl-app
```

## 📝 Резюме команд для первого запуска

```bash
# 1. Перейдите в директорию проекта
cd ~/snap/knigavl.ru

# 2. Установите зависимости
npm install

# 3. Соберите фронтенд
npm run build

# 4. Запустите через PM2
pm2 start ecosystem.config.js

# 5. Сохраните конфигурацию PM2
pm2 save

# 6. Проверьте статус
pm2 status

# 7. Посмотрите логи
pm2 logs vl-app
```

## ✅ Готово!

После выполнения всех шагов:
- Node.js сервер работает на порту 10001
- Telegram боты активны и отвечают на сообщения
- React фронтенд собран и готов к раздаче через Nginx
- MySQL база данных подключена
- PM2 управляет процессом и автоматически перезапускает при сбоях

---

**Контакты для поддержки:**
- Telegram: @VL_TEX_BOT
- Канал: @VLTOKEN

